/* // primitive types
        // boolean
        var boolean = false;
        console.log(`The variable ${boolean} has type ${typeof(boolean)}`);

        // number
        var number = 1;
        console.log(`The variable ${number} has type ${typeof(number)}`);

        // string
        var name = 'diana';
        console.log(`The variable ${name} has type ${typeof(name)}`);

        // function
        var function = function(){};
        console.log(`The variable ${function} has type ${typeof(function)}`);


        // how to declare

        var; // global or local variable - null initial value

        let; // local block variable - null initial value

        const; // block local variable - required initial value */

        
        // using var global
        /* var name = 'diana';
        var name = 'Diana';

        // using var local
        functionFunctionName() {
            var last name = 'Martine';
            console.log(last name);
        }

        console.log(name);
        FunctionName(); */
        // var properties apply to let, but by convention we use let in local scope


        /* // using const
        constant constant = 'constant';
        // value cannot be changed; */



        /* // assignment
        var assignment = 'diana';

        // Comparation
        var comparison = "0" == 0;
        console.log(comparison);

        // identical comparison
        var identicalcomparison = "0" === 0;
        console.log(identical comparison); */


       /* // arithmetic operators
        // addition
        var addition = 1 + 1;
        console.log(addition);

        //subtraction
        var subtraction = 2 - 1;
        console.log(subtraction);

        //multiplication
        var multiplication = 2 * 3;
        console.log(multiplication);

        // actual division
        var realDivision = 6 / 2;
        console.log(realdivision);

        // integer division
        var integerDivision = 5 % 2;
        console.log(IntegerDivision);;

        //potentiation
        var power = 2**10;
        console.log(power); */


       /* // relational operators
        // bigger then
        var greaterthan = 5 > 2;
        console.log(greaterthan);
        
        // less than
        var less than = 5 < 2;
        console.log(less Than);
        
        // greater than or equal to
        var greaterOrEquals = 5 >= 2;
        console.log(greaterOrEquals);
        
        // less than or equal to
        var lessOrEquals = 5 <= 2;
        console.log(lessOrEquals); */


       /* // logical operators
        // && - all values ​​must be true
        var e = true && true;
        console.log(e);

        // || - only one value must be true
        var ou = true || false;
        console.log(or);

        // ! - inverts values
        var no = !true;
        console.log(no); */
