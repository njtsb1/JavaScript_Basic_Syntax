// what are vectors or arrays

/* // how to declare an array
let array = ['string', 1, true];
// must be declared between square brackets "[]";
console.log(array); */

// how an array can hold different values ​​including other arrays
//let array = ['string', 1, true, ['array1'], ['array2'], ['array3'], ['array4']];

/* // how to access the index of an array
console.log(array[0]); */


// manipulating an array

//forEach - performs a function for each array item
//array.forEach(function(item, index){console.log(item, index);})

//push - add item to end of array
//array.push('new item');
//console.log(array);

//pop - remove the last item from the array
//array.pop();
//console.log(array);

//shift - removes the first item from the array
//array.shift();
//console.log(array);

//unshift - add item at the beginning of the array
//array.unshift('new item');
//console.log(array);

//indexOf - returns the index of an array item
//console.log(array.indexOf(true));

//splice - remove or replace item by index
//array.splice(0, 3);
//console.log(array);

//slice - returns a part of an existing array
//let newArray = array.slice(0, 3);
//console.log(newArray);


/* // what are objects
// how to declare an object
let object = { property1: 'value1', property2: 'value2' }
// must be declared between curly braces "{}";
console.log(object); */

// just like arrays, object properties can hold any values
// let object = { string: 'string', number: 1, boolean: true, array: ['array'], InnerObject: { InnerObject: 'Inner object' } };

/* // how to access object properties
// using dot "."
console.log(object.internal object); */

// manipulating object
// destructuring object
/* let string = object.string;
let number = object.number;
let boolean = object.boolean; */


// unstructuring using curly braces
// let { string, number, boolean } = object;
// console.log(string, number, boolean);